<%= simple_form_for [current_user, @order] do |f| %>
  <% if @order.errors.any? %>
    <% @order.errors.each do |name, error| %>
      <%= error %>
    <% end %>
  <% end %>
  <%= f.input :food_id, collection: Food.all, label_method: :name %>
  <%= f.time_select :arrival_time, minute_step: 5, start_hour: 7 , end_hour: 13, ampm: true %>
  <%= f.button :submit %>
<% end %>
