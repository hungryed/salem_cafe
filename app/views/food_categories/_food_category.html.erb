<div class="small-12 medium-6 small-centered columns">
  <%= simple_form_for [@section, @food_category] do |f| %>
    <%= f.input :name %>
    <%= f.input :multiple, label: 'Customer can have multiple in an order' %>
    <%= f.input :description %>
    <%= f.button :submit %>
  <% end %>
</div>
