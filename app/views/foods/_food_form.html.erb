<%= simple_form_for @food do |f| %>
  <%= f.input :name %>
  <%= f.input :section_id, label: 'Section', collection: Section.all, label_method: :name %>
  <%= f.input :description %>
  <%= f.input :picture, as: :file %>
  <%= f.button :submit %>
<% end %>
